# Soulforge Progress — Scheduled Tasks

## Completed
- **STORY-1** (committed: daaccc6): Schema migration (0039), Drizzle schema fields, shared types, validators, issue create/update routes accept schedule fields, GET /api/projects/:id/schedules endpoint, tests.

## Remaining
- **STORY-2**: Scheduler tick service — `tickScheduledTasks(db, heartbeat, now)`, clone template→instance, wake integration, wire into server tick loop
- **STORY-3**: UI Schedules tab on project board, template filtering from Issues tab

## Key Files Modified (STORY-1)
- `packages/db/src/migrations/0039_scheduled_issue_templates.sql`
- `packages/db/src/schema/issues.ts` — schedule/template fields
- `packages/shared/src/types/issue.ts` — Issue type extended
- `packages/shared/src/validators/issue.ts` — schedule field validation
- `server/src/services/issues.ts` — create/update handle schedule fields
- `server/src/routes/projects.ts` — GET /api/projects/:id/schedules
- Tests in `server/src/__tests__/issues-*.test.ts` and `projects-schedules-route.test.ts`

## Constraints
- DO NOT create a separate routines/schedules table
- DO NOT add templateId back-reference
- DO NOT modify adapter code
- Use existing cron-parser package
- Target patches branch
